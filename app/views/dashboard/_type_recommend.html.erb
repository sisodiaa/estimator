<div class="row my-4">
	<h2 class="font-weight-light text-muted">Belt recommendation(s) for <%= metadata %></h2>
</div>
<div class="row">
	<table class="table table-striped">
		<thead class="thead-dark">
			<tr>
				<th scope="col">Belt (Grade)</th>
			</tr>
		</thead>

		<tbody>
			<%= render partial: 'type_recommend_result', collection: results, as: 'result' %>
		</tbody>
	</table>
</div>
