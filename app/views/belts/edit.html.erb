<%= render partial: "shared/breadcrumbs", object: @breadcrumbs, as: 'items' %>
<div class="row">
	<div class="col-6 card">
		<h2 class="mt-2">Editing Belt Record</h2>
		<hr/>

		<%= render 'form', belt: @belt %>

		<div class="btn-group">
			<%= link_to 'Show Record', @belt, class: 'btn btn-block btn-outline-primary my-3' %>
			<%= link_to 'Back to all records', belts_path, class: 'btn btn-block btn-outline-primary my-3' %>
		</div>
	</div>
</div>
