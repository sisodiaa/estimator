<%= render partial: "shared/breadcrumbs", object: @breadcrumbs, as: 'items' %>

<% if @result.result %>
	<%= render partial: @result.template, locals: { results: @result.result, metadata: @result.metadata } %>
<% end %>
