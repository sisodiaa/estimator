<div class="row">
	<div class="col-6">
		<h1>Editing Record</h1>
		<hr/>

		<%= render partial: 'form', locals: { mill: @mill, mill_machine: @mill_machine, url: mill_machine_form_path(@mill_machine) } %>

		<%= link_to 'Show', @mill_machine, class: 'btn btn-outline-primary mt-3' %>
		<%= link_to 'Back', mill_machines_path, class: 'btn btn-outline-primary mt-3' %>
	</div>
</div>
