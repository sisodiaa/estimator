<%= render partial: "shared/breadcrumbs", object: @breadcrumbs, as: 'items' %>

<div class="row">
	<div class="col-6 card">
		<h2 class="mt-2">Add Belt to Machine</h2>
		<hr/>

		<%= render 'form', machine_belt: @machine_belt, url: machine_belts_path(@machine) %>

		<%= link_to 'Back to all records', machine_belts_path(@machine),
			class: 'btn btn-outline-primary my-3' %>
	</div>
</div>
